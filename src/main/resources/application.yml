server:
  port: 8080

spring:
  application:
    name: BfStatsApplication
  thymeleaf:
    cache: false
    check-template: true
    check-template-location: true
    prefix: classpath:/templates/
    enabled: true
    encoding: UTF-8
    suffix: .html
